from pathlib import Path

# Define the base directory of the project (the directory containing this script)
BASE_DIR = Path(__file__).resolve().parent

# Define the path to the data directory
PLANNING_DATA_DIR = BASE_DIR / 'skills' / 'planning' / 'data'